<?xml version='1.0' encoding='utf-8' ?>
<RelativeLayout android:id="@id/grid_view_root"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:paddingStart="?attr/preferenceItemPaddingStart"
                android:paddingEnd="?attr/preferenceItemPaddingEnd" xmlns:android="http://schemas.android.com/apk/res/android">
  <com.android.settings.sound.coolsound.RingtoneGridView android:id="@id/grid_view"
                        android:scrollbars="none"
                        android:layout_width="fill_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginBottom="@dimen/unlock_card_margin_edge_interval"
                        android:listSelector="@android:color/transparent"
                        android:horizontalSpacing="@dimen/unlock_card_margin_edge_interval"
                        android:verticalSpacing="@dimen/unlock_card_margin_edge_interval"
                        android:numColumns="2" />
</RelativeLayout>